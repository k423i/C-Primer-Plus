#include <bits/stdc++.h>
using namespace std;
int main(){
	int x=1000000007;
	char s[100000];
	int p[100000],a[100000],t[100000];
	int q=0,w=0,e=0;
	memset(p,0,100000*4);
	memset(a,0,100000*4);
	memset(t,0,100000*4);
	gets(s);
	for(int i=0;i<100000;i++){
		if(s[i]=='P') p[i]++;
		else if(s[i]=='A') a[i]++;
		else t[i]++; 
	}
	
	for(int i=0;i<100000;i++){
		if()
	}
	return 0;
} 
